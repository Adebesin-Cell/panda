const examplePreset = {
  name: '@panda-test/with-preset-export/preset',
  theme: {
    extend: {
      tokens: {
        colors: {
          namedBrand: { value: '#aabbcc' },
        },
      },
    },
  },
}

module.exports = { examplePreset }
