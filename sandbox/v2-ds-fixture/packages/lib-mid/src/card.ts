import { css } from '../styled-system/css'
import { card } from '../styled-system/recipes'

export const cardClass = card()

export const midHero = css({
  bg: 'brand',
  color: 'surface',
  p: '6',
  borderRadius: '12px',
})
