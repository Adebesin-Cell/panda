import { css } from '../styled-system/css'
import { panel } from '../styled-system/recipes'

export const panelClass = panel()

export const leafHero = css({
  bg: 'accent',
  color: 'brand',
  p: '10',
  fontSize: '24px',
})
